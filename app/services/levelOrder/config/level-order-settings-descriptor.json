{
  "properties": {
    "name": "Level orders"
  },
  "options": {
    "defaults": {
      "description": "Default level order values",
      "maxLot": {
        "type": "number",
        "description": "Default max lot per child order; 0 disables splitting"
      },
      "minLot": {
        "type": "number",
        "description": "Default minimum lot/quantity step; 1 keeps whole-number sizing"
      },
      "stopOffsetPts": {
        "type": "number",
        "description": "Default stop offset from level in points"
      },
      "takeProfitPts": {
        "type": "number",
        "description": "Optional default take profit in points"
      },
      "buyPriceSource": {
        "type": "string",
        "enum": ["bid", "ask", "mid"],
        "description": "Price source for LB button"
      },
      "sellPriceSource": {
        "type": "string",
        "enum": ["bid", "ask", "mid"],
        "description": "Price source for LS button"
      }
    },
    "symbols": {
      "description": "Ticker-specific level order defaults",
      "item": {
        "description": "Ticker defaults",
        "ticker": {
          "type": "string",
          "description": "Ticker"
        },
        "maxLot": {
          "type": "number",
          "description": "Max lot per child order; 0 disables splitting"
        },
        "minLot": {
          "type": "number",
          "description": "Minimum lot/quantity step; 1 keeps whole-number sizing"
        },
        "stopOffsetPts": {
          "type": "number",
          "description": "Stop offset from level in points"
        },
        "takeProfitPts": {
          "type": "number",
          "description": "Optional take profit in points"
        },
        "buyPriceSource": {
          "type": "string",
          "enum": ["bid", "ask", "mid"],
          "description": "Price source for LB button"
        },
        "sellPriceSource": {
          "type": "string",
          "enum": ["bid", "ask", "mid"],
          "description": "Price source for LS button"
        }
      }
    }
  }
}
