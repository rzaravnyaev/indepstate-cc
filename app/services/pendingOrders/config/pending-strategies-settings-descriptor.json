{
  "properties": {},
  "options": {
    "consolidation": {
      "description": "Consolidation breakout settings",
      "bars": {
        "type": "number",
        "description": "Number of bars to consider"
      },
      "rangeRule": {
        "type": "string",
        "description": "Range Rule"
      },
      "dealPriceRule": {
        "type": "string",
        "description": "Deal Price Rule"
      },
      "stoppLossRule": {
        "type": "string",
        "description": "Stop loss rule (B1_TAIL or LEVEL_OFFSET; LEVEL_OFFSET uses the card SL as offset points)"
      }
    },
    "falseBreak": {
      "description": "False break strategy settings",
      "tickSize": {
        "type": "number",
        "description": "Tick size for breakout"
      }
    },
    "limitByCurrent": {
      "description": "Limit by current price strategy",
      "stoppLossRule": {
        "type": "string",
        "description": "Stop loss rule name (B1_TAIL or LEVEL_OFFSET; LEVEL_OFFSET uses the card SL as offset points)"
      },
      "priceSource": {
        "type": "string",
        "description": "Price source to use (bid|ask|mid)"
      },
      "historyBars": {
        "type": "number",
        "description": "Number of historic bars to load"
      },
      "historyTimeframe": {
        "type": "string",
        "description": "Timeframe for historic bars"
      }
    }
  }
}
